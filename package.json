{
  "name": "grras-solutions",
  "version": "1.0.0",
  "description": "GRRAS Solutions Training Institute - Complete Website",
  "main": "backend/server.py",
  "scripts": {
    "dev": "concurrently \"cd backend && python -m uvicorn server:app --reload --host 0.0.0.0 --port 8001\" \"cd frontend && yarn start\"",
    "build": "cd frontend && yarn install && yarn build",
    "start": "cd backend && python -m uvicorn server:app --host 0.0.0.0 --port ${PORT:-8001}",
    "railway:build": "cd backend && pip install -r requirements.txt",
    "railway:start": "cd backend && python -m uvicorn server:app --host 0.0.0.0 --port $PORT"
  },
  "keywords": [
    "education",
    "training",
    "institute",
    "fastapi",
    "react",
    "mongodb",
    "cms"
  ],
  "author": "GRRAS Solutions",
  "license": "MIT",
  "engines": {
    "node": ">=18.0.0",
    "python": ">=3.11.0"
  },
  "repository": {
    "type": "git",
    "url": "https://github.com/your-username/grras-solutions.git"
  }
}